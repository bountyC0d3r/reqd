---
name: "reqd: Task"
description: "Work on a task with full spec context — AI-guided, grounded in signed-off requirements"
category: reqd
tags: [requirements, lifecycle, spec-driven]
---

Invoke the `reqd-task` skill with the argument: $ARGUMENTS
