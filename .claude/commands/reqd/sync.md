---
name: "reqd: Sync"
description: "Check if the Jira ticket has changed since the spec was locked — surface a diff and record the scope decision"
category: reqd
tags: [requirements, lifecycle, jira]
---

Invoke the `reqd-sync` skill with the argument: $ARGUMENTS
